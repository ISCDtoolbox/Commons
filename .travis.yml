language: c

os:
  - linux
  - osx

compiler:
  - gcc
  - clang

before_install:
#  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then brew update        ; fi
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then brew install libomp; fi

sudo: false

script: 
  - mkdir build
  - cd build
  - cmake ..
  - make
  - rm -r ./*
  - cmake .. -DOPENMP=1
  - make

notifications:
  email: false

