language: c

os:
  - linux
  - osx

compiler:
  - gcc
  - clang

script:
  - lsb_realease -a
  - gcc --version
  - make --version
  - cmake --version
  - git --version
  - dpkg -l | grep libgomp1
  - dpkg -l | grep libpthread-stubs0-dev
  - mkdir build
  - cd build
  - cmake ..
  - make
  - cd ..
  - rm build -dr
  - mkdir build
  - cd build
  - cmake .. -DOPENMP=1
  - make

notifications:
  email: false

